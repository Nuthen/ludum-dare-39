-- Put settings crap in here like keybinds or whatever
return {
    skipSplashAndMenu = true,
    CANVAS_WIDTH = 640,
    CANVAS_HEIGHT = 360,

    soundVolume = 1.0,
    musicVolume = 0.3,
    maxSoundsPlaying = 5,
}
